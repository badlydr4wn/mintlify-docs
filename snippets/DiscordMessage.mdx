export const FormattedMessage = ({ avatar, username, timestamp, app }) => (
<div style={{ padding: "0.25rem", margin: "0.5rem", color: "#ffffff", display: "flex", backgroundColor: "#2b2d31", borderRadius: "0.25rem" }}>
  <div style={{ paddingRight: "0.75rem", transform: "translateY(-0.25rem)" }}>
    <img style={{ height: "2.5rem", borderRadius: "50%" }} src={avatar} alt="Avatar" />
  </div>
  <div>
    <div style={{ display: "flex", alignItems: "flex-end" }}>
      <p style={{ marginRight: "0.5rem", fontWeight: "600" }}>{username}</p>
      <p style={{ textTransform: "uppercase", fontSize: "0.75rem", fontWeight: "900", backgroundColor: "--primary", padding: "0.25rem", borderRadius: "0.25rem" }}>APP</p>
      <p style={{ marginLeft: "0.5rem", color: "#9ca3af", fontSize: "0.75rem" }}>{timestamp}</p>
    </div>
    <div>
      <p>Hello world</p>
    </div>
  </div>
</div>
);
