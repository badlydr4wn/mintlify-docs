export const FormattedMessage = ({ avatar, username, timestamp, type }) => (
  <div className="flex rounded" style={{backgroundColor: "#00000", color: "#fffff"}}>
    <img src={avatar} alt={username} />
    <div>
      <div className="flex">
        <p>{username}</p>
        <p >{type}</p>
        <p>{timestamp}</p>
      </div>
      <div>
        hello world
      </div>
    </div>
  </div>
);
